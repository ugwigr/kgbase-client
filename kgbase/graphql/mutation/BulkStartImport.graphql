mutation BulkStartImport(
  $context: GraphContextType!,
  $bundleId: String!,
  $sheetConfigs: [SheetConfigType!]!,
) {
  bulkStartImport(
    context: $context,
    bundleId: $bundleId,
    sheetConfigs: $sheetConfigs
  ) {
    ok
    taskId
  }
}
